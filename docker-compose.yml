version: "3.9"

services:
  data-copilot:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    container_name: Data-Copilot-container
    restart: unless-stopped
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=11440
